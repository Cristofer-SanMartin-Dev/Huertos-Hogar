# Copiar a ".env" y ajustar según el entorno. Vite solo expone al cliente
# las variables que empiezan con VITE_ (nunca pongas secretos acá: terminan
# en el bundle público del navegador).

# URL base de la API del backend.
VITE_API_BASE_URL=http://localhost:8080
